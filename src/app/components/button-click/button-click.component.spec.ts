import { ButtonClickComponent } from './button-click.component';

describe('ButtonClickComponent', () => {
  let component: ButtonClickComponent;

  beforeEach(() => {
    component = new ButtonClickComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
